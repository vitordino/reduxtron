{
  "name": "reduxtron",
  "version": "0.0.6",
  "author": "vitordino",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -rf ./dist",
    "build": "tsc -b -v && cpy --flat 'src/*.ts' 'dist'",
    "postbuild": "ts-node-esm src/setupPackage.ts",
    "release": "npm run build && cd dist && npm publish"
  },
  "sideEffects": false,
  "dependencies": {
    "electron": ">=24.0.0",
    "redux": ">=4.0.0"
  },
  "peerDependencies": {
    "zustand": ">=4.0.0"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "ts-node": "^10.9.1",
    "typescript": ">=4.9.5"
  }
}
